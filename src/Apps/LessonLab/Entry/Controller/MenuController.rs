

pub struct MenuController {
	// create new material 
	// exit app
	// item list
}

impl MenuController {
	pub fn exit_app() {
		// i think we need a global nga exit_app() nya mo call ta ana nga function diri
		// why we need a global one because this might not be the only exit for our app
		// NOTE: maybe naa ang dioxus ana **maybe**
		// save current state
		// then exit
	}
}